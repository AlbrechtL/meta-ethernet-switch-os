BBPATH .= ":${LAYERDIR}"

BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "rtl83xx-distro"
BBFILE_PATTERN_rtl83xx-distro = "^${LAYERDIR}/"
BBFILE_PRIORITY_rtl83xx-distro = "7"

# Deliberately no dependency on rtl83xx-bsp: the image .bbappend only applies
# where the BSP's rtl83xx-image-initramfs recipe exists.
LAYERDEPENDS_rtl83xx-distro = "core openembedded-layer swupdate"
LAYERSERIES_COMPAT_rtl83xx-distro = "wrynose"

# The image .bbappends have no recipe to attach to when the BSP layer is absent,
# and the .swu recipes package the BSP's rtl83xx-image; only parse them with it.
BBFILES_DYNAMIC += " \
    rtl83xx-bsp:${LAYERDIR}/dynamic-layers/rtl83xx-bsp/*/*/*.bb \
    rtl83xx-bsp:${LAYERDIR}/dynamic-layers/rtl83xx-bsp/*/*/*.bbappend \
"
